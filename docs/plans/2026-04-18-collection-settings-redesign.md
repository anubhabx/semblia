# Collection Settings Redesign

- [ ] Add failing tests for the new config ownership split and settings route behavior.
- [ ] Refactor the collect config types and local stores so builder-owned concerns are separate from project settings.
- [ ] Reduce the collect inspector to respondent-facing tabs and add a settings handoff.
- [ ] Build the new project settings shell with collection-focused sections and structured future tabs.
- [ ] Update the preview so it derives branding defaults and settings-owned behavior cleanly.
- [ ] Run focused tests, typecheck, index updates, and the `web_v2` build before closing the task.